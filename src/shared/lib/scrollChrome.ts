// Shared scroll thresholds so the navbar (slides out) and the site dock
// (slides in once the navbar is fully gone) stay in sync.
export const NAV_HIDE_THRESHOLD = 80;
export const NAV_HIDE_DURATION_MS = 700;
